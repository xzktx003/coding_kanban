# Product

## Register

product

## Users

需要在本机或可信内网中同时管理多个 CLI Coding Agent、tmux、文件和 VS Code Web 会话的开发者。用户通常在高信息密度的大屏工作台中持续观察、切换并接管多个长时间运行的任务。

## Product Purpose

Coding Kanban 把多 Agent 观察、真实终端输入、tmux 接入、文件浏览和编辑器操作压缩成一个连续工作流。成功意味着用户可以快速判断任务状态、找到目标会话并准确把输入发送到当前控制的终端，同时保持历史工作现场可恢复。

## Brand Personality

技术化、克制、可靠。界面应像可信的开发工具，优先保证信息层级、状态辨识和连续操作效率，而不是展示性装饰。

## Anti-references

- 不采用高饱和霓虹、彩虹式无语义配色或大面积装饰渐变。
- 不采用低对比度、标题与正文难以区分的扁平层级。
- 不让动画、弹层或额外导航结构遮挡终端和高频操作。
- 不为视觉新奇重新发明标准按钮、折叠、菜单和焦点交互。

## Design Principles

- 让终端和会话始终是视觉与交互中心。
- 用稳定、克制的视觉编码帮助用户定位分组、状态和当前输入目标。
- 在主看板、聚焦侧栏和移动端复用一致的组件语义。
- 高信息密度不能牺牲可读性、可点击性和明确反馈。
- 破坏性、接管和更新操作必须显式呈现并由用户确认。

## Accessibility & Inclusion

交互状态不能只依赖颜色表达；文字、图标、边框或形状应提供同步线索。文本与控件保持足够对比度，支持键盘焦点和语义化 ARIA，并遵循 `prefers-reduced-motion` 降级动画。
