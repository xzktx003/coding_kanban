# Coding Kanban 功能清单

本文档按当前仓库实现整理功能范围，作为后续新增功能时必须同步维护的清单。历史计划文档仅作背景，本清单以当前代码、现有接口和已经落地的交互为准。

## 1. 会话看板与聚焦工作流

- 主页把未隐藏的会话自动分入“需响应 / 执行中 / 待验收 / 可继续”四列，并在列头显示数量：明确等待用户回答、权限或确认的 `awaiting_input` 会话进入“需响应”，`running` 进入“执行中”，刚从运行态完成且尚未查看的 `idle` / `exited` 会话进入“待验收”，已查看的完成会话以及 `detached` 会话进入“可继续”。查看“需响应”只打开上下文，不会清除等待输入状态；执行新命令或恢复运行后进入“执行中”。桌面端四列并排，窄屏纵向排列。
- 主页宫格工具栏提供“按状态 / 按分组”切换：按状态继续使用四个状态列，并在列内保留用户分组；按分组则把用户组直接作为一级分区，同组内的需响应、执行中、待验收和可继续会话集中展示，未指定归属的会话进入“未分组”。两种视图的折叠状态互不干扰，所选分区方式保存在浏览器本地。
- 完成待验收状态由后端随会话快照持久化，不依赖单个浏览器页面；双击看板卡片、切换聚焦会话、激活多终端窗格或在手机端打开会话均视为已查看。
- 每个分区展示会话卡片的名称、状态、Agent 类型、主机、工作目录和轻量终端文本预览；受管 tmux 使用独立的小号 `tmux` 标签，不把传输类型拼进显示名称。按状态视图的用户分组嵌套在各状态列内，按分组视图则直接按组集中排列。
- 本机 Codex 卡片以及绑定本地 tmux 的 `shell` / `node` 兼容卡片从结构化 JSONL 会话记录中提取最后一条用户指令和最后一条 Agent 回复，经过空白清理、Markdown 标记移除和长度截断后，以“任务 / 回复”摘要展示在终端片段上方；明确的 OpenCode、Claude、Copilot 等非 Codex 会话不会读取同目录 Codex JSONL。没有结构化记录或远端会话继续只显示终端预览。
- 本地会话卡片按工作目录读取只读 Git 摘要，以固定高度单行展示项目名、当前分支或 detached HEAD、worktree 标记、变更文件数及已跟踪内容的增删行数；未跟踪文件计入文件数但不计增删行。非 Git 目录显示项目名和“非 Git 工作目录”，远端会话显示“远端 Git 信息暂不可用”。
- 宫格看板支持按最近活动、项目和名称切换分区内排序；排序不会打乱四个状态列或用户分组的顺序，选择保存在浏览器本地。
- 完成态宫格卡片支持主动标记已读/未读；未读状态进入“待验收”，并通过后端会话状态持久化。
- 支持按服务器、Agent 类型、tmux 类别、目录关键字筛选。
- 支持隐藏、恢复、删除、重命名、关闭/脱离、终止 tmux、复制 tmux attach 命令。
- 看板筛选行保留已隐藏会话入口；状态数量由四列列头统一展示，不重复显示旧版统计徽标。
- 双击卡片进入聚焦视图，在主终端直接继续输入。
- 聚焦视图支持单屏、左右双屏、上下双屏、左中右三屏、四屏、六屏、八屏终端监控模式；多列布局按主终端区域的真实宽度响应，六屏会稳定保持三列两排，只有终端区小于 `720px` 时才切换为单列。多窗格只用于同时观察，键盘输入只发送到带高亮边框和“当前输入”醒目角标的一个窗格，不支持广播输入。点击窗格、选择不同会话或点击侧栏会话后，当前输入窗格、顶部标题以及文件/VS Code 工具上下文保持同步；在非输入窗格重复选择其当前会话只关闭菜单，不抢占输入。
- 每个监控窗格标题栏的会话切换器按看板当前分组顺序展示可选会话，并复用稳定分类色、组名和数量；分组标题可独立折叠/展开并保留名称与数量，折叠状态与看板、聚焦侧栏分开持久化，搜索时匹配分组自动保持展开。当前会话显示“当前”，已在其他窗格展示的会话显示对应窗格编号且不可重复选择，未分组会话保持独立分区。分组内容超出弹层高度时由外层列表统一滚动，不裁切组内选项；当前分组标题到达顶部后保持吸顶，直到下一分组标题向上滚动并替代它。
- 用户分组按配置顺序稳定使用醒目的高对比色，前 12 个分组使用命名分类色，更多分组继续生成不同的 HSL 色值；主页状态列、聚焦侧栏和多终端切换器对同一分组显示同一颜色，不再依赖容易碰撞的短哈希色板。分组归属同时保留稳定会话 ID、Agent 会话 ID 和安全的 tmux 身份别名，应用重启或运行时元数据变化后继续恢复。
- 聚焦视图多屏模式下，仅当前输入窗格的标题栏支持右键菜单，可执行单屏展示、关闭看板展示该窗口或彻底删除该终端；“关闭看板展示该窗口”会从当前窗格移除该会话，并优先用第一个未上屏会话补位；进入单屏展示后，同一右键菜单首项会替换为“还原多屏展示”，恢复进入单屏前的窗格布局；终端内容区和非选中窗格不触发该自定义菜单。
- 聚焦视图右侧现有分组列表保留全部可见会话；受管 tmux 小卡在真实标题后显示低对比度 `tmux` 标签。已进入大屏布局的会话小卡显示对应的 1-based 窗格编号，当前输入窗格与对应小卡同步显示黄色高亮。点击带编号小卡只切换当前输入，不移动分屏；点击未编号小卡继续替换当前输入窗格。
- 聚焦视图侧栏单击会立即把会话切换到当前主输入窗格；双击不会重复切换。单屏模式保留当前会话及最近两个 xterm 实例，来回切换时复用现有终端、WebSocket 和 scrollback；同一页面内从多屏缩到单屏时，已实际打开的自由排列窗格会以隐藏实例保留（最多八个），再次展开不重建 xterm、不重连 WebSocket，也不重新回放历史。隐藏期间跳过零尺寸 fit/resize，避免改写 tmux 终端尺寸。聚焦确认使用有序的轻量 `204` 请求，最终状态通过现有会话增量通道同步，不再用完整 HTTP 快照触发全部卡片重渲染。
- 多屏模式点击左右窗格只更新本地 `activeSlotId`、输入所有权和顶部活动标题；文件、VS Code 或变更面板打开时，才同步 App 级 focused session 和对应工具上下文。无侧栏时不发送额外 `/focus` 请求，避免看板快照和终端视觉刷新。
- 多屏区域按自身容器宽度响应式重排；窄于 `720px` 转为单列时不再把全部窗格压进当前高度。左右双屏和三屏保留一屏高的窗格，四/六/八屏保留原多列布局的半屏高度，并由多屏区域显示独立纵向滚动条。普通滚轮优先翻动溢出的窗格列表，`Shift+滚轮`继续交给当前终端历史，Ctrl/Command 组合不被布局接管。
- 聚焦视图右侧“全部会话”卡片支持右键菜单，可执行关闭看板展示该窗口或彻底删除该终端；关闭看板展示会隐藏该会话但不终止底层终端。
- 聚焦视图支持鼠标拖拽排布：右侧全部会话卡片可拖入任意分屏窗格，分屏窗格头部可相互拖动并交换会话位置。
- 聚焦视图保留全部可见会话上下文，右侧会话侧栏使用轻量终端文本预览并支持折叠；当会话数量较多时，卡片会先压缩到指定最小高度，超过后侧栏固定在聚焦页高度内并通过内部滚动条浏览。

## 2. 顶栏入口与快捷键

- 顶栏按“端切换品牌 / 主操作 / 当前会话工具 / 工具菜单 / 资源调节 / 窗口控制”分组：桌面标题显示为“电脑端 Coding Kanban”，旁边提供“手机端 Coding Kanban”切换入口；常驻新建会话、扫描菜单、文件、VS Code、工具菜单、资源调节、全屏和折叠入口；扫描 tmux 与扫描会话收纳到“扫描”，操作提示收纳到“工具”。
- 菜单栏折叠状态持久化到本地存储。
- 开发入口默认使用 HTTPS 协议；`restart-dev.sh` 输出局域网可访问的 HTTPS 前端地址，并自动准备开发证书。
- 顶栏常驻“终端字号”滑杆，支持在 10px 到 24px 之间拖动调整所有内置 xterm 终端及“完整记录”正文的字号，并持久化到本地存储。
- 顶栏“工具”菜单提供 Agent 完成通知开关；浏览器授权后，已知会话从 `running` 进入 `idle` 或 `exited` 时会发送系统通知，提示当前看板任务已经完成并需要查看。
- 顶栏“资源调节”菜单提供终端预览模式切换，可在默认轻量预览和旧版完整小终端预览之间切换，并收纳 VS Code 省内存/保持状态、释放 VS Code 缓存和资源诊断。
- 顶栏“资源调节”菜单提供资源诊断面板，按需展示 xterm 实例数、终端 WebSocket 数、会话快照吞吐、终端实时流吞吐、终端历史缓冲裁剪状态、VS Code iframe 当前/隐藏数量、主线程长任务、VS Code 代理 HTTP/WS 吞吐和 Chromium JS heap 指标，用于定位浏览器内存、网络增长、长输出丢失与 VS Code iframe 卡顿来源。
- 提供 `Ctrl/⌘+E` 快速连接 tmux、`Ctrl/⌘+Shift+S` 打开 tmux 扫描、`Alt+Q` 从聚焦视图返回宫格等快捷操作。

## 3. 新建会话

- 支持本机和 SSH 主机两类目标。
- Agent 类型支持 `copilot`、`codex`、`claude`、`shell`。
- 启动方式支持 `direct` 和 `tmux`。
- 新建本地或 SSH 会话默认选择受管 `tmux` 模式；`direct` 仍可显式选择，但应用更新或后端重启后只能保留卡片元数据，不能恢复原 PTY。
- 显示名称留空时，前端根据主机、Agent 类型和启动方式自动生成唯一默认名。
- 工作目录输入支持目录建议和目录选择器；目录选择器可浏览本机或 SSH 目标目录，并可在当前文件夹下新建文件夹后直接选为会话工作目录。
- 本地会话通过 `/api/agent-launch/pty` 启动，远端会话通过 `/api/agent-launch/ssh-pty` 启动。

## 4. tmux 接入、扫描与管理

- 支持本地 tmux 扫描。
- 支持 SSH 远端 tmux 扫描。
- 支持快速连接本机或远端 tmux，会话存在即 attach，不存在即创建。
- 支持将已运行的 tmux pane 接入为可交互 PTY。
- 支持对 tmux 会话执行 `refresh`、`takeover`、`release`、`kill`。
- tmux 扫描、快速连接和加入看板时，卡片显示名称使用真实 tmux session 名，不再生成 `tmux:<session>`、`tmux:<session> (<command>)` 或远端路径后缀；命令、目录、主机和 tmux 绑定分别保存在现有结构化字段中，内部 runtime id 仍可使用 `tmux:` 命名空间。新建或恢复受管 tmux 时，服务端按 tmux 规则把传输目标中的 `.`、`:` 规范为 `_`，但保留用户输入的卡片显示名称，避免目标名不一致导致键盘输入失效。
- 状态文件读取时只迁移可确定为旧版系统生成的 tmux 标题，用户自定义标题和非 tmux 标题保持不变。

## 5. Agent 工作目录扫描

- 支持扫描本地目录与 SSH 远端目录。
- 支持识别 Copilot `session-state`。
- 支持把扫描结果与 tmux pane 进行合并，减少重复卡片。
- 扫描结果支持按 direct 或 tmux 模式加入宫格。

## 6. 终端交互与 WebSocket

- 聚焦视图顶栏新增“变更”面板，明确分为“本次任务”和“当前工作区”两个独立 Diff 语义，并默认打开信息更完整的当前工作区。当前工作区基于可信会话工作目录读取 Git 已跟踪文件的修改、暂存、新增、删除、重命名和冲突，也逐文件展示未被 `.gitignore` 排除的 `??` 新文件及其 `/dev/null → 文件` Diff，支持文件筛选、增删统计和复制路径。每个 `@@` 改动块在鼠标悬停或键盘聚焦时显示“还原此改动”，经不可撤销确认后只反向应用该块并保留同文件其他修改；后端同步处理该块在暂存区和工作区中的内容，新增或未跟踪文件的唯一改动块还原后可能删除文件。本次任务支持本机 Codex 及运行 Codex 的本地 tmux，会先解析卡片对应的精确 Codex session，再按最新 `turn_id` 直接映射原生 `FileChange`（新增、删除、修改和重命名），旧版记录继续以最新用户任务之后的 `apply_patch` 作为兼容兜底；没有可靠记录时明确提示不可归因，不用当前 Git Diff 冒充 Codex 改动。
- 手机终端页增加全屏“变更”视图，支持本次任务/当前工作区切换、下拉选择变更文件、简化统一 Diff、复制路径和当前工作区逐改动块还原；触屏设备不依赖悬停，直接显示改动块还原按钮；不提供 stage、commit、merge 等仓库级写操作。
- 手机端默认直接进入“当前会话”终端控制页，立即展示已选会话；当前会话选择条使用醒目的聚焦终端样式并同时显示状态。底部“看板”仍按“需响应 / 待验收 / 执行中 / 可继续”的注意力顺序展示全部会话：四个状态标题都是整行触控折叠按钮，可各自收起或展开所属终端卡片，收起后继续显示状态名称、说明、数量和方向提示；页面内切换底部模块不会丢失折叠状态。分组标题和卡片复用桌面端的稳定状态色、强调条及数量徽标，卡片在手机宽度内紧凑显示任务/回复、Git 分支与增删统计、末尾三行轻量终端内容。卡片只消费已有会话快照，不为每个会话创建真实终端、WebSocket 或额外摘要请求。
- 手机端提供固定主导航：看板、活动、当前会话、项目/文件；活动按最近更新时间展示摘要，项目/文件按主机和项目目录聚合会话入口，并可直接进入文件系统。文件系统复用本地/SSH 文件接口，支持目录导航、当前目录搜索、隐藏文件开关、可纵向触摸滚动的文本与图片预览、复制路径；路径工具栏提供“新建”入口，可在当前目录创建空文件或文件夹，并拒绝空名称、`.`、`..` 和包含路径分隔符的名称。新建菜单使用页面内原生输入框，并在打开时立即聚焦以可靠唤起手机输入法，不依赖可能被浏览器拦截的系统提示框。Markdown 与电脑端复用安全的 GFM/KaTeX 渲染链并适配窄屏表格、代码和图片，同时提供“渲染 / 源码”查看方式切换。文件路径、查看方式和分段导航默认收进可折叠的“文件选项”，让正文优先占用手机视口；大型 UTF-8 文件按 64 KiB 窗口流式读取，可用“上一段 / 下一段”继续阅读，切换查看方式或翻段时页面始终只保留当前窗口。文件列表长按 600ms 会打开触屏底部操作菜单，手指滑动会取消长按；菜单复用现有本地/SSH 文件能力，支持进入/预览、下载、重命名、删除和复制路径，删除前必须再次确认。

- 聚焦主终端使用 xterm.js 渲染，后端通过 WebSocket 发送 scrollback replay 与实时输出。
- 电脑端聚焦页支持把剪贴板截图或本地 PNG、JPEG、WebP 图片发送到当前输入窗格对应的 Codex 对话：用户可直接在终端粘贴图片，也可点击标题栏“图片”选择文件；发送前显示目标会话、缩略图和可编辑说明，可用显式“取消”放弃发送，失败后保留原图与原文供重试。单张限制 10 MB，服务端校验真实文件头；本地会话使用仅当前进程可读的临时文件，SSH 会话使用目标主机 `~/.cache/coding-kanban/codex-images` 临时目录。服务端优先调用 `codex queue --thread ... -i ...`；当目标主机的 CLI 版本实际拒绝 queue 图片附件时，自动改投带绝对临时路径的文字消息，让对应 Codex 通过图片查看工具读取，并将图片延迟最多 24 小时清理；相同目标的后续发送会复用能力探测结果。原生附件投递完成、发送失败或取消确认时不保留临时图片。本地 tmux 会先解析当前活动 pane 的精确 Codex session，不向同目录的其它对话误发；远端会话使用登记在卡片上的 Codex session ID。当前窗格不是 Codex、主机缺少 `codex` 命令或 SFTP 不可用时明确报错。手机端现有交互保持不变。
- 聚焦页标题栏提供“完整记录”入口：多屏模式下入口持续绑定带“当前输入”标记的活动窗格，点击终端内容（包括 xterm 的 helper textarea）、“设为输入”或侧栏卡片切换当前窗格后，标题、入口会话 ID 和已打开弹窗的数据源同步切换。本机 Codex、明确登记为 Codex 的本地 tmux 兼容会话，以及具备 `sshTarget` 的远端 Codex/tmux 兼容会话都可读取对应主机的 Codex JSONL，按时间正序展示用户消息、助手回答及非 `exec` 工具记录，最新记录位于底部，不受 TUI 光标移动、擦除行或终端重绘影响；OpenCode、Claude、Copilot 等明确非 Codex 会话不读取同目录记录。`exec` 的调用与输出均不展示。用户与 Codex 发言统一使用安全 Markdown/GFM/KaTeX 渲染，并在接近可视区域时才懒加载；长记录由服务端从 JSONL 文件尾部按字节游标分段读取，首次只返回最新约 30 条记录，向上滚动接近顶部或点击“加载更早记录”时再返回约 30 条较早记录，不再读取、解析或传输整份历史；加载旧页后保持当前滚动锚点。前端轻量模式最多保留当前连续的 90 条，完整预览模式最多保留 300 条，超出窗口时释放较新记录并可通过刷新回到最新，避免内存随浏览深度持续增长。工具输出保持折叠的等宽原文，可按需展开和手动刷新；本地 tmux 会先从对应 pane 的进程树识别当前 Codex 持有的顶层 session JSONL，并排除子代理记录，因此多个 tmux 即使共享工作目录也各自显示对应历史；无法解析进程身份时才按已绑定 session ID 或工作目录最近记录兜底。远端会话使用登记的 SSH/SFTP 通道读取目标主机文件，不回退到服务端本机历史。
- 当前实现已补充 SSH 远端 Codex 与远端 tmux 兼容会话：通过登记的 `sshTarget` 在目标主机读取 `~/.codex/sessions`，按 session ID 或工作目录匹配，并使用 SFTP 有界字节窗口分页；远端请求不会回退读取服务端本机文件。
- 终端历史保留上限可配置：后端 live PTY replay 默认保留 4 MiB，tmux observe/refresh 默认捕获最近 20000 行，registry fallback 默认保留 5000 条输出记录，前端 xterm 默认保留 20000 行；tmux attach 会在连接前把 pane 历史预灌到 PTY replay，资源诊断会提示 PTY replay 是否已发生裁剪。手机端建立终端连接时通过受限的 `replayBytes` 参数只请求最近 256 KiB，服务端校验为正整数并强制不超过配置的全局回放上限；桌面端继续使用既有完整回放。
- 所有内置 xterm 终端共用顶栏字号设置；滑杆拖动中只更新顶栏显示，鼠标松开、键盘调整结束或失焦提交后才对聚焦主终端、多屏监控终端、完整预览终端和手机端终端生效，并重新 fit/resize。
- 提供手机端终端控制页，默认入口为 `/?view=mobile`，兼容 `/mobile`、`/m` 和 `#/mobile`；进入后默认直接展示单会话全屏终端，复用现有会话和终端通道，以底部快捷键条和多行输入框为主，解决手机缺少 `Ctrl+C`、`Esc`、方向键、稳定软键盘输入和终端历史滑动的问题。手机终端在挂载时完成一次不启用 stdin 的光标初始化，并使用高对比度下划线光标；输入框或快捷键持有页面焦点时仍显示 TUI 当前编辑位置，初始化后会立即归还此前的页面焦点；快捷键条提供”说明”按钮，可弹出各快捷键用途说明；说明弹窗支持 `aria-modal`、`aria-labelledby`、Tab 聚焦陷阱和 Escape 关闭。
- 手机端会话标题区使用单实例页面内列表切换终端，列表支持点外部或 Escape 关闭，不依赖会被实时快照反复触发的浏览器原生选择层；可选会话固定按“可继续 → 执行中 → 其它状态”分组，每组按名称的中文拼音或字母顺序稳定排列，不跟随活动时间跳动。“完整记录”“变更”和“文件”保持在同一行操作组，其中完整记录与桌面聚焦视图复用同一记录弹窗，“文件”直接打开当前会话的文件系统并可一键返回终端。
- 手机端快捷键条发送通用终端控制字符，不提供 tmux 专用快捷键专区，也不把低频按键收进“更多”；所有按键保持在同一横向常驻行，按 `Shift / Esc / Ctrl+C / Enter / Tab / 方向键 / 退格 / 组合键 / 说明` 排列。工具栏提供一次性 `Shift` 修饰键；方向键和退格只有静止按住满 3 秒才开始连续发送，每次等待上一笔 stdin 请求完成后再继续，松手后不会残留输入队列。
- 手机端输入框只保留“发送”和“粘贴”两个动作：两者都使用 bracketed paste 安全写入完整文本，只有“发送”再单独发送一次真实 `Enter`；两帧任务提交如果中途失败，会保留草稿、显示重试，并只续发失败帧，避免重复粘贴已经成功写入的内容。
- 手机端标题区提供同一套 Agent 完成通知开关；手机浏览器支持并授权通知时，页面保持打开即可在任务完成后收到系统通知。
- 手机端终端页锁定浏览器根页面滚动，终端区域用捕获阶段的非 passive touch 监听接管上下滑动，触屏设备在桌面聚焦页也启用同一终端手势控制，避免 Codex 长上下文下拉时触发浏览器下拉刷新；单指滑动滚动 xterm scrollback，双指 pinch 调整终端字号，并提供”底部”按钮返回最新输出。
- 手机端快捷键工具栏保持单行横向选择器布局，用户即使从方向键或退格上开始手势也可顺畅左右滑动；滑动超过触摸容差会取消待触发按键和长按计时。按键触摸高度至少 44px，重复键长按时阻止系统文本选择和触摸呼出菜单。字号持久化到 localStorage，reload 后保持上次缩放级别。
- 默认轻量预览模式下，宫格卡片和聚焦右侧栏不打开真实终端 WebSocket，只展示轻量文本预览；预览会清理 ANSI 和终端字符集切换，并忽略纯光标、擦除、边框绘制块，避免可读内容被空白终端帧覆盖，同时避免多会话时浏览器内存和网络流量随卡片数量线性膨胀。
- 可切换到完整预览模式，恢复宫格卡片和聚焦右侧栏的旧版小终端预览。
- 前端会对会话快照 WebSocket、终端实时 WebSocket、挂载中的 xterm/终端视图和 JS heap 做轻量采样，资源诊断面板只在打开时刷新，避免诊断自身形成持续负载。
- 实时终端 WebSocket 异常断开后会按 250ms 到 5 秒的有界退避自动重连；如果代理让连接永久停在 `CONNECTING`，3 秒后会主动回收并走同一恢复链。连接完成历史回放后自动恢复 resize、焦点和键盘输入，不再要求用户刷新整个看板。
- 后端会对高频终端输出触发的看板状态做约 1 秒的合并广播；每条 WebSocket 连接首次收到完整快照，后续只收到变化会话的 upsert、已删除 ID、当前焦点和更新时间，前端在内存中合并回完整状态。结构性操作仍即时刷新，同时避免任意一个终端输出时向手机反复传输全部会话。
- 支持 replay 完成前缓冲 live frame，避免历史输出与新输出乱序。
- 支持 stdin、resize、binary 消息，binary 用于 tmux 鼠标等二进制事件。
- 直接 PTY 会按会话串行 stdin，并在输出 `CSI c`、`CSI 5n`、`CSI 6n` 查询后短暂等待匹配的 DA、DSR、CPR 回复；陈旧或类型不符的浏览器回复不会写入 shell。这样普通文本不会与终端协议字节交错，Copilot/Codex/OpenCode 启动后的快速输入不会丢字或变成命令前缀。鼠标移动/hover 报告属于终端控制流，不记为用户输入，也不刷新看板预览。
- 服务端生成的 tmux attach 命令和带显式命令的 direct PTY 使用非交互 `/bin/sh` 执行，不加载用户 shell 启动文件；未指定命令时仍启动用户原生交互 shell。
- 本地 tmux 在 attached client 的 PID 被 `tmux list-clients` 确认前，不会把首个输入误写进即将 `exec tmux attach` 的启动 shell；输入短暂排队，超时则安全回退到 pane 的 `send-keys`。确认后普通文本、方向键、bracketed paste、鼠标点击/滚轮协议和 `Ctrl+A` / `Ctrl+B` 前缀都按原始字节经同一个有序 PTY 输入，因此始终跟随浏览器当前可见 pane；tmux client 不支持的 CSI-u 修饰键例外走 `send-keys -l`，以保留原始字节。
- `Ctrl+A` / `Ctrl+B` 后会按当前 tmux `prefix` key table 判断下一键是否绑定 `command-prompt` / `confirm-before`；`:`、`,`、`$`、`'`、`.`、`/`、`f` 及自定义 prompt 的文本和编辑键持续写入 attached client PTY，直到 Enter 提交或 Ctrl+C/连接清理取消。`status-keys vi` 下 Escape 只切换编辑模式，不再误判为 prompt 已退出；裸 Ctrl+C 始终经过 attached client，因此服务重载后即使内存状态丢失也能取消残留 prompt，无 prompt 时由 tmux 正常转发给当前 pane。
- 后端收到 SIGTERM/SIGINT 时先关闭 Fastify，并由 `PtyRuntimeManager.dispose()` 终止该进程创建的全部本地/SSH PTY；源码热重载不再遗留 PPID=1 的 `tmux attach` clients。
- 桌面 xterm 把 macOS `Option` 和 Windows/Linux `Alt` 统一编码为终端 Meta 修饰键；`Option+Space` / `Alt+Space` 及常用 Meta 字母、数字组合在本地 tmux 中作为单个 `M-*` 键发送，不再拆成 `Escape` 与普通字符。Windows 系统若优先占用 `Alt+Space`，可使用跨平台 `Shift+Enter` 换行。
- Safari 快速输入时同时核对 xterm `onData` 与浏览器原生 `insertText`；若 WebKit 已产生文本输入但 xterm 没有发出对应数据，只补发缺失字符。该恢复路径仅在 Safari 启用，并排除 IME composition、控制键及其他浏览器。
- `Shift+Enter`、`Ctrl+Enter` 的 CSI-u 序列在 tmux client 不支持 extended keys 时通过 `send-keys -l` 原样写入当前 pane，不依赖不同 tmux 版本对 `S-Enter` / `C-Enter` 键名的支持。
- 支持终端输出发起的 OSC 52 剪贴板写入；tmux copy-mode 可通过 pane 内选择把内容写入浏览器剪贴板，前端只接受 clipboard target 且限制 payload 大小。OpenCode 活动终端默认让普通左键拖拽建立本地选区并复制，避免 mouse tracking 把复制手势回灌为模型输入；单击和滚轮仍保留 TUI 操作。
- 支持终端焦点补救和输入所有权；真实 stdin 默认只落到当前聚焦主终端，多终端监控模式下也只落到当前输入窗格。交互终端开启 mouse tracking 时，普通滚轮作为鼠标协议发送到当前 tmux/TUI pane；`Shift+滚轮`、未开启 mouse tracking 的大屏终端和非输入监控窗格继续滚动各自 xterm scrollback。聚焦页右侧会话小卡始终是被动预览，滚轮穿透小终端并滚动“全部会话”侧栏，不控制预览内部历史。

## 7. 文件浏览器

- 聚焦视图下支持打开文件浏览器。
- 支持本地文件系统与 SSH/SFTP 远端文件系统。
- 支持面包屑、返回上级目录、显示隐藏文件、过滤、排序。
- 文件列表展示名称、大小、修改时间、Owner、权限，并支持拖动表头列间分割线调整各列宽度。
- 支持文本预览和编辑保存；单击文件继续在文件列表下方显示紧凑预览，双击文件或点击“编辑”会把左侧文件系统面板切换为全高的内联阅读/编辑视图，并提供“返回文件列表”，不再用覆盖弹层遮挡右侧终端。电脑端任一已加载文件还可点击“全屏预览”，在浏览器完整视口内复用当前文本、图片或 Markdown 预览状态，并可通过“退出全屏”或 `Esc` 返回文件面板；手机端文件系统交互保持不变。
- `.md` / `.markdown` 文件默认展示渲染结果，并可在同一左侧面板内切换预览、源码编辑和实时分屏；全高预览提供可折叠标题目录，目录项使用稳定锚点跳到对应章节，每段目录最多挂载 500 项以限制异常文档的 DOM。实时分屏默认双向同步源码与渲染区的相对滚动位置，也可切换为独立滚动。预览支持 GitHub Flavored Markdown、标题、列表、任务列表、表格、代码块、安全外链，以及 `$...$` / `\(...\)` 行内公式和 `$$...$$` / `\[...\]` 块级 LaTeX 公式，内嵌 HTML 不执行。Markdown 中的本地或 SSH 相对图片、项目根相对图片及根目录内绝对图片通过受限资源接口读取；图片接近视口时才流式加载为 Blob，单张上限 16 MiB，失败可重试，关闭或切换预览后立即释放对象 URL。HTTP(S) 图片继续使用浏览器懒加载。Markdown/KaTeX 渲染引擎按需加载，预览正文记忆化，分屏编辑使用延迟内容刷新，仅在显式保存时写入文件。电脑端大型 Markdown 按最多约 1 MiB 的滑动窗口读取，可前后换段且仅保留当前正文；只有文件完整载入时才允许编辑保存，避免用截断内容覆盖原文件。手机端现有 64 KiB 分段交互保持不变。
- 支持新建文件/目录、重命名、删除、chmod、上传、下载、拖拽上传。
- 右键菜单支持上传到当前目录或选中目录、下载、重命名、删除、复制路径、chmod。

## 8. VS Code Web

- 本地与 SSH 聚焦会话都支持打开内嵌 VS Code Web 面板。
- 本地后端优先复用 `code-server`，其次支持 `openvscode-server`；SSH 远端首版通过 SSH 启动/复用远端 `code-server`。
- `/vscode` WebSocket 代理在独立的 code-server 上游握手完成前使用 1 MiB 有界队列保留浏览器首包，连接后按原顺序转发，避免扩展宿主握手被静默丢弃。
- 本地会话使用稳定的 `.code-workspace` 文件；SSH 远端会话直接打开远端工作目录。
- 扩展目录优先复用用户的 `~/.vscode-server/extensions`。
- 打开 iframe 前先注册并立即注销隔离的 Service Worker 探针；远端浏览器未信任开发 CA 时不加载 VS Code，而是显示 CA 公有证书下载、macOS/Safari 信任步骤和重新检测按钮，私钥不会暴露给前端。
- VS Code/文件面板与主终端之间的分隔条使用 pointer capture，拖入 iframe 后仍能继续调整；拖动中的宽度更新按动画帧合并且不触发整棵 React 树和 localStorage 写入，松手时只持久化最终宽度。主终端在拖动期间延后 fit/resize，避免 code-server iframe 和 xterm 同时高频重排。
- 浏览器侧 iframe 默认使用 VS Code 省内存模式，只保留当前打开的 iframe；可切换到保持状态模式，最多保留最近 3 个 iframe。
- 支持一键释放 VS Code iframe 缓存，卸载非当前 iframe 以回收浏览器内存。

## 9. SSH 与环境适配

- 远端主机列表直接读取并解析当前用户的 `~/.ssh/config`。
- 本地与远端 shell 选择优先使用当前环境 `SHELL`，不足时按 `bash -> zsh -> sh` 回退。
- tmux 路径支持 `TMUX_BINARY`、Homebrew 路径和 `PATH` 自动探测。
- 新建 SSH 终端会话前会通过交互式远端 shell 预检工作目录、所选 Agent 命令以及 tmux 模式需要的 `tmux`；检查失败时不创建无效卡片，并在新建窗口显示可操作的具体原因。
- SSH 进程在预检后异常退出时仍保留 exited 会话、终端末尾输出和退出码，便于直接查看远端运行期错误并显式重连。

## 10. 布局与状态持久化

- 顶栏折叠、文件浏览器布局、聚焦视图状态、侧边工具选择等 UI 状态保存在本地存储。
- 文件/VS Code 侧面板的拖动宽度只在 pointer release 时提交到 `file-browser-ui-state`；中间帧直接更新面板 DOM，刷新后仍恢复最后一次完成的宽度。
- 会话分组配置保存在 `coding-kanban-session-groups-v1`：主页看板与聚焦视图“其他会话”侧栏共享分组名称和卡片归属，刷新页面后保持。
- 新建会话窗口支持选择“未分组”或已有会话分组；会话创建成功后立即按稳定会话标识写入所选分组，创建失败不会产生分组归属。
- 每个会话分组（包括自动生成的“未分组”）都可折叠或展开；主页四个状态栏分别保存各自的分组折叠状态，同一分组在一个状态栏收起不会联动其他状态栏。聚焦侧栏继续独立保存自己的折叠状态，所有状态均与分组配置一起持久化。
- 用户分组标题按稳定分组标识分配琥珀、青绿、天蓝或珊瑚分类色，并使用更大的粗体标题、整行浅色背景和同色数量徽标增强定位；主看板与聚焦侧栏保持同一色调，“未分组”固定使用中性色，识别不只依赖颜色。
- 前端提供轻量 UI 动效：顶栏菜单、资源诊断、主机下拉、卡片、抽屉和弹窗使用 opacity/transform 入场与悬停过渡，并遵循 `prefers-reduced-motion` 降级。
- 聚焦视图终端监控布局模式保存在本地存储，重新进入时保持 `屏幕布局` 菜单中的单屏、左右双屏、上下双屏、左中右三屏、四屏、六屏或八屏选择。
- 聚焦视图还会保存每个监控 slot 的稳定 session id、当前输入 slot 和用户主动关闭的 slot；应用更新 reload 后按仍存在的稳定 session id 恢复原分屏现场。
- 文件浏览器按会话和主机维度保存独立浏览状态。
- VS Code Web 会话缓存支持跨切换复用。
- VS Code iframe 缓存模式保存在本地存储，默认省内存；自动超时卸载暂未默认启用，保留为下一阶段策略。

## 11. 应用更新检测与历史会话恢复

- 后端通过 `GET /api/app-version` 暴露当前进程 runtime id、启动时间、Git branch/head、本地源码指纹和自动更新状态；指纹覆盖 tracked 修改、未跟踪文件、commit、checkout 和 pull 后的 HEAD 变化。tracked diff 使用流式哈希，未跟踪文件按总预算有界读取；并发版本查询会复用同一次指纹计算和短期缓存。
- `GIT_AUTO_PULL_INTERVAL_MINUTES` 可设置为 `10`、`30` 或 `0`。启用后，独立 `GitAutoUpdateService` 在启动时立即检查并按配置周期固定执行 `fetch --prune`，只更新 `available` 提醒状态，绝不由定时器执行 pull 或 merge；定时器和手动操作通过 single-flight 串行化。
- 检测到远程新版本时，前端显示可关闭的“拉取并更新”提示。提示是非模态的，只有更新和关闭按钮接收指针事件，不会遮挡底下终端或顶栏控件。只有用户点击确认后，后端才通过 `POST /api/app-update/apply` 尝试 `merge --ff-only <remote-head>`；成功后自动保存恢复意图、reload 并恢复受管 tmux，不再要求第二次确认。
- 用户确认拉取后，如本地未提交修改会被覆盖、存在未跟踪同名文件、分支已经分叉或 fast-forward 被 Git 拒绝，后端保持 HEAD 和工作区不变，前端显示“检测到新版本，但存在冲突”，仅允许再次显式确认重试拉取。网络、上游或凭证错误显示独立检查失败提示。
- 前端每 3 秒检查版本。源码 revision 变化时只显示“检测到新版本 / 更新并恢复”，不会在用户输入终端时自动刷新；提示可主动关闭，同一 revision 在后续轮询和 reload 后保持隐藏，新的 revision 会重新提示。
- 点击“更新并恢复”会接受当前 revision、记录一次性恢复意图并 reload；同一 revision 不会形成刷新循环。
- 后端把稳定会话目录持久化到 `SESSION_STATE_PATH`，默认 `.dev-runtime/agent-sessions.json`。只保存 session id、显示信息、目录、tmux/SSH 绑定、标签和当前卡片等元数据，不保存 terminal output、PTY PID 或 runtime id。
- 后端重启后，`POST /api/agent-sessions/restore-managed` 只重新 attach 仍存在的受管 tmux 会话；tmux 已不存在时显示失败，不会根据旧命令重建。direct 会话保留为需要手动恢复的 exited 卡片。多标签页并发恢复会在后端合并为同一次执行。
- 恢复成功提示支持主动关闭，并在出现后 5 秒内自动隐藏；恢复失败提示保留在页面上，避免错误信息来不及查看。
- `restart-dev.sh` 在停止旧后端前读取 `/api/agent-sessions`，用于首次升级时把旧版内存注册表迁移到状态文件；迁移时立即剔除 terminal output、PTY PID、runtime id 和其他瞬态字段。只有目标端口存在本仓库后端且捕获失败时才拒绝重启；无本仓库监听器或只有外部监听器时不会误判为迁移失败。
- `restart-dev.sh` 在计算后端和前端端口默认值前，把 `.env` 按 dotenv 的 `KEY=value` 数据读取，不执行其中的 shell 片段；未加引号的空格值可被保留，带引号值会去除外围引号，`SERVER_PORT`、`PORT` 和 `WEB_PORT` 会按该配置生效，格式错误会在停止任何服务前明确失败。
- 会话状态文件只在持久化元数据实际变化时原子写入，终端输出快照、连接运行态或单纯 `updatedAt` 变化不会造成持续落盘；写入失败只记录后端错误，不中断看板服务。
- Git 更新测试使用临时 bare remote 和两个 clone，覆盖后台只 fetch/check 且不改 HEAD、用户确认后的 clean fast-forward、未提交修改冲突、分支分叉、并发 single-flight、10/30 分钟定时器和手动接口；隔离 E2E 继续使用临时 Git 根目录、状态文件、前后端端口和 tmux socket，覆盖提醒、确认、冲突到自动会话恢复的完整链路。

## 12. Paper Writer 项目工作区

- 当前运行的 Paper Writer 构建产物中，项目侧栏只展示两类项目入口：`开放项目` 和 `归档项目`。
- `开放项目` 只包含未归档且未放入回收站的项目；`归档项目` 只包含已归档且未放入回收站的项目。
- 旧入口 `所有项目`、`我的项目`、`回收站` 不再作为侧栏项目分类展示；删除、归档、取消归档等项目操作仍保留在项目行内。
- 文章编辑页的预览窗口在 `pdf`、`diff` 同级提供 `翻译` 按钮，可直接调用现有大模型接口翻译当前渲染预览对应的稿件内容，并在预览区域显示译文、加载态和错误信息。
- 文章编辑页右侧 AI 区域中，`Skill` 不再作为与 `Chat` 并列的顶层 tab 展示；它绑定在 chat 窗口内，点击 chat 头部的 `Skill` 按钮会展开 skill 选项，选择后直接创建并进入带该 skill 的 chat 会话。
- Chat 输入区支持两种大模型接入模式：默认 `Built-in LLM` 继续走现有 `/api/ai/send`、`/api/ai/stream` 后端；`Bash Agent` 模式允许配置 `copilot`、`codex`、`claude` 等 bash 命令，并随 chat 请求把 `mode` 和 `bashCommand` 发送给后端执行层。
- 当前运行的 Paper Writer 静态构建入口包含前端资源自动同步逻辑，会轮询已加载的 `/assets/*.js` 和 `/assets/*.css` 资源签名；运行产物变化后页面会自动刷新，避免每次改前端后都需要手动刷新浏览器。

## 13. PM 审计增强 (2026-06-16)

### 13.1 宫格空态引导

- 空态下显示三步快速入门指引：新建会话 → 双击进入聚焦 → 快捷键操作。
- 保留原有新建会话和扫描 tmux 按钮。

### 13.2 聚焦视图折叠标题栏增强

- 标题栏折叠时显示状态徽标（running/idle/detached/exited）和 agentKind 标签。

### 13.3 快速连接 tmux 记忆历史

- 连接成功后自动保存最近连接记录（最多 8 条）到 localStorage。
- 下次打开快速连接弹窗时，在主机搜索前显示"最近连接"快捷入口。
- 点击最近连接条目可一键填充主机、会话名和工作目录。

### 13.4 聚焦视图侧栏会话搜索

- 当"其他会话"超过 2 个时，侧栏标题下方显示搜索输入框。
- 支持按会话名称、Agent 类型、工作目录模糊过滤。

### 13.5 会话标签/分组支持

- `AgentSessionRecord` 新增可选 `tags: string[]` 字段。
- FilterBar 新增标签筛选器（仅在存在标签时显示）。
- 卡片 footer 显示用户自定义标签（蓝色标签样式）。

### 13.6 文件浏览器拖拽路径到终端

- 文件浏览器中的文件和目录条目支持拖拽。
- 拖拽到聚焦视图的终端区域时，自动将文件路径写入终端输入并追加空格。

### 13.7 Shell 工具函数共享

- `shellQuote` 和 `formatWorkingDirectory` 提取到 `packages/shared/src/shell-utils.ts`。
- 前端和后端统一从 `@agent-orchestrator/shared` 导入，消除三处重复实现。

### 13.8 看板会话分组

- 主页看板工具栏和聚焦视图“其他会话”侧栏都支持新建分组。
- 创建第一个分组后，两处视图按同一分组顺序展示卡片；未指定归属的卡片自动进入“未分组”。
- 每张主页卡片和侧栏卡片都提供“移动到分组”选择菜单，可移动到任意分组、回到“未分组”，或新建分组并立即移入。
- 用户分组支持重命名和删除；删除分组不会删除会话，其中的卡片自动回到“未分组”。
- 分组归属优先使用 Agent session id 或 tmux pane 等稳定标识，避免卡片改名后丢失分组。
- 分组标题提供展开/折叠箭头；折叠后保留分组名称和卡片数量，只隐藏该组卡片，刷新页面和切换主页/聚焦视图后保持状态。
- 主页可在状态一级分区和用户组一级分区之间切换；用户组视图按配置顺序展示组内全部状态的会话，并使用独立的 `group` 折叠作用域，刷新页面后恢复上次选择。
- 分组标题使用居中、加粗的代码风格字形和分组专属高亮色，左侧色带、光晕边框与计数徽标共同建立明显的视觉层级；看板主列和聚焦侧栏分别采用适配屏幕宽度的字号。
- 多终端窗格标题栏的会话切换器复用同一分组顺序、名称和稳定分类色，并提供独立于主页看板和聚焦侧栏的分组折叠状态；折叠后保留组名与数量，只隐藏组内会话，不会被侧栏的折叠操作联动。
- 每个监控窗格标题栏的当前会话名称使用更大的可点击按钮，标题文本在按钮中居中、下拉箭头固定在右侧；当前输入窗格使用橙色高亮和更强字重，按钮提示明确说明点击后切换对应窗格会话。
- 多终端窗格的会话切换器打开后支持搜索会话名和分组名；搜索期间匹配分组自动保持展开且暂时禁用折叠，命中分组名时保留该组全部会话，命中会话名时只显示对应会话，并继续保留分组层级、占用窗格标记和当前输入状态。

### 13.9 热更新共享包同步

- 后端开发进程启动前自动构建当前工作区的 `@agent-orchestrator/shared`，确保共享源码与运行时 `dist` 一致。
- `tsx` 同时监听 `packages/shared/src`；拉取只涉及共享包的远程更新时，也会触发后端重启和会话恢复链路。
- 检测到本地或远程新版本时，页面左上角只显示 `24px` 状态灯，不再用大横幅遮挡工作区；状态灯常驻直到用户点击，单击即执行对应的更新/拉取动作。冲突和检查失败使用警告色，完整原因保留在按钮提示与无障碍名称中。

### 13.10 多 tmux pane 的 Codex 历史跟随

- 完整记录根据 Kanban PTY 对应的 tmux client 和当前活动 pane 解析 Codex session，不再把注册时的固定 pane 当作永久历史来源。
- 活动 pane 没有 Codex 时不回退到上一个 pane；后端重载、Kanban client 正在重连时仍按 tmux session 查询当前活动 pane，只有 tmux 查询失败时才使用固定 pane、已知 session ID 或工作目录匹配作为兼容回退。
- 完整记录弹窗打开期间低频探测 session ID，切换 tmux pane 后自动替换历史；同一 session 的手动分页内容不会被无意义重置。

### 13.11 活动 tmux pane 的 Codex 目录绑定

- 有活动 Kanban tmux client 时，完整记录解析以当前活动 pane 的真实进程目录为准，不再使用卡片首次发现时遗留的工作目录。
- 同一 tmux session 切换到不同工作目录的 Codex 后，打开句柄和关闭句柄回退均绑定当前 pane 的顶层 Codex session。

### 13.12 tmux 鼠标拖动选择与复制

- 本地 tmux 的 xterm 鼠标按下、拖动和释放报告经 attached PTY 有序转发，tmux `mouse on` 下的 pane 切换、copy-mode 拖选和复制保持原生语义。
- 无按钮 hover motion 报告（含修饰键变体）继续过滤，避免高频 hover 把会话误标为输入或污染 TUI；按住按钮的拖动 motion 不过滤，tmux copy-mode 通过既有 OSC 52 处理写入浏览器剪贴板。当前可控的大屏 xterm 同时屏蔽浏览器原生右键菜单，保证 tmux 右键绑定收到完整鼠标报告。

### 13.13 分组监控排列

- 聚焦视图的“屏幕布局”菜单同时提供“自由排列”和“分组排列”；选择分组后展示该组全部会话窗格，单屏选项禁用并自动使用多屏布局。当前窗格和可视区域附近挂载真实 xterm，离屏窗格降级为轻量预览并在滚动进入视口时自动恢复，避免大分组同时建立全部终端 WebSocket。
- 分组会话超过当前布局容量时，仍按所选列数连续向下铺开；普通鼠标滚轮由外层监控区域按帧合并后浏览布局，允许停在两批窗格之间，不会把滚轮误用于某个 xterm 的历史回滚。
- 分组模式下按住 Shift/⇧ 再滚轮可切换为当前 xterm 的内部历史滚动；Ctrl/⌘ 组合不被布局捕获，保留浏览器缩放等原生行为。
- 分组排列下窗格不可拖拽替换，窗格标题切换器只展示当前分组；选择已显示的同组会话会直接激活对应窗格，不能跳到其他分组。
- 分组排列状态、分组 ID 和自由排列槽位分别持久化，退出分组排列后恢复用户原有的自由排列工作区。
